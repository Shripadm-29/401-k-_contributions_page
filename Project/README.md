# 401(k) Contribution App (Flask Version)

## How to Run

1. Install dependencies:
run 'pip install flask' on the terminal

2. Start the server:
run 'python3 app.py' on the terminal

3. Open your browser:
click this link to access the application: http://127.0.0.1:5000
